# don't run this too often or you may get dropped by the mirrors
*/30 * * * * root /usr/local/bin/apt-fetch
# cleanup cache for packages which have been installed
0 */12 * * * root /usr/local/bin/apt-fetch -p
